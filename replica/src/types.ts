export interface Tool {
  category: string;
  name: string;
  href: string;
  description: string;
  rank: number;
  domain: string;
  logo: string;
}

// export type ToolsData = Record<string, Tool[]>;